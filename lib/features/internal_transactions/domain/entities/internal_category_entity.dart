class InternalCategoryEntity {
  final int id;
  final String name;
  final bool isActive;

  const InternalCategoryEntity({
    required this.id,
    required this.name,
    required this.isActive,
  });
}